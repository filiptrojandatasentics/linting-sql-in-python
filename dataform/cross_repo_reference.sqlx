config { type: "table", schema: "marketing_marts" }

select
    m.*
    , s.revenue_data
from ${ref("marketing_leads")} as m
left join `my-project.sales_marts.customer_revenue` as s  -- Reference sales repo output
    on m.lead_id = s.customer_id
